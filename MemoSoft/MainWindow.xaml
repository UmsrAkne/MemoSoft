<Window x:Class="MemoSoft.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MemoSoft"
        xmlns:vms="clr-namespace:MemoSoft.ViewModels"
        mc:Ignorable="d"
        Activated="App_Activated"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vms:MainWindowViewModel/>
    </Window.DataContext>

        <Grid Name="parentGrid" Opacity="0.8">


            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

        <Menu VerticalAlignment="Top" Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="Save"/>
                <MenuItem Header="Quit"/>
            </MenuItem>
        </Menu>

        <TextBox x:Name="textBox"
             Margin="0"
             AcceptsReturn="True"
             FontSize="12pt"
             Padding="3"
             Foreground="Black"
             Opacity="0.8"
             Text="{Binding PostgreSQLDatabaseHelper.EnteringComment,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
             Grid.Row="1"
         >
            <TextBox.InputBindings>
                <KeyBinding Key="Return" Modifiers="Ctrl" 
                            Command="{Binding PostgreSQLDatabaseHelper.InsertCommentCommand}"
                            CommandParameter="{Binding Text, ElementName=textBox}"
                />

            </TextBox.InputBindings>
        </TextBox>
        
    </Grid>

    <Window.Resources>

        <Style x:Key="{x:Type ListBoxItem}"
               TargetType="ListBoxItem">
          <Setter Property="SnapsToDevicePixels"
                  Value="true" />
          <Setter Property="OverridesDefaultStyle"
                  Value="true" />
        </Style>
    </Window.Resources> 
    
</Window>
